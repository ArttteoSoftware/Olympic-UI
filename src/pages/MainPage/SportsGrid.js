import Card from "../../components/MiniCard/MiniCard";
import styles from "./SportsGrid.module.css";

function SportsGrid() {
	return (
		<div className={styles.container}>
			<Card />
		</div>
	);
}

export default SportsGrid;
